import React from 'react';

import CompaniesList from './CompaniesList';

const Companies = () => {
    return (
        <>
            <CompaniesList />
        </>
    );
};

export default Companies;
